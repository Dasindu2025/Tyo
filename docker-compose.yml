version: '3.8'

services:
  app:
    container_name: worktime-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://root:Dasindu2025@tyotrack-tyotrack-42xw30:5432/tyotrack
      JWT_SECRET: ${JWT_SECRET:-your-super-secret-jwt-key-change-this-in-production-min-64-chars}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-your-super-secret-refresh-key-different-from-jwt-min-64-chars}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:3000}
    restart: unless-stopped
    networks:
      - worktime-network

networks:
  worktime-network:
    driver: bridge
